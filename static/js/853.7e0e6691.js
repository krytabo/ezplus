"use strict";
(self["webpackChunkvue3_ezplus_official_website"] = self["webpackChunkvue3_ezplus_official_website"] || []).push([[853],{

/***/ 8853:
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  "default": function() { return /* binding */ NewsContent2; }
});

// EXTERNAL MODULE: ./node_modules/@vue/runtime-core/dist/runtime-core.esm-bundler.js
var runtime_core_esm_bundler = __webpack_require__(9812);
// EXTERNAL MODULE: ./node_modules/@vue/shared/dist/shared.esm-bundler.js
var shared_esm_bundler = __webpack_require__(5893);
// EXTERNAL MODULE: ./node_modules/@vue/runtime-dom/dist/runtime-dom.esm-bundler.js
var runtime_dom_esm_bundler = __webpack_require__(4220);
;// CONCATENATED MODULE: ./node_modules/babel-loader/lib/index.js??clonedRuleSet-40.use[0]!./node_modules/vue-loader/dist/templateLoader.js??ruleSet[1].rules[3]!./node_modules/vue-loader/dist/index.js??ruleSet[0].use[0]!./src/components/NewsContent2.vue?vue&type=template&id=62ffce64

const _hoisted_1 = {
  class: "w-full bg-white"
};
const _hoisted_2 = {
  class: "pt-16"
};
const _hoisted_3 = {
  class: "py-10 px-8 lg:px-12 xl:px-52",
  style: {
    "background": "#f9fafb"
  }
};

const _hoisted_4 = /*#__PURE__*/(0,runtime_core_esm_bundler/* createTextVNode */.Uk)("首頁");

const _hoisted_5 = /*#__PURE__*/(0,runtime_core_esm_bundler/* createTextVNode */.Uk)("最新消息");

const _hoisted_6 = {
  class: "mb-4 h-full text-left text-4xl font-bold"
};
const _hoisted_7 = {
  class: "flex flex-col space-x-0 text-left text-gray-600 sm:flex-row sm:space-x-4"
};
const _hoisted_8 = {
  class: "flex items-center"
};

const _hoisted_9 = /*#__PURE__*/(0,runtime_core_esm_bundler/* createElementVNode */._)("i", {
  class: "ri-time-fill mr-1 text-base"
}, null, -1);

const _hoisted_10 = {
  class: "flex items-center"
};

const _hoisted_11 = /*#__PURE__*/(0,runtime_core_esm_bundler/* createElementVNode */._)("i", {
  class: "ri-map-pin-2-fill mr-1 text-base"
}, null, -1);

const _hoisted_12 = {
  class: "flex items-center"
};

const _hoisted_13 = /*#__PURE__*/(0,runtime_core_esm_bundler/* createElementVNode */._)("i", {
  class: "ri-shield-user-fill mr-1 text-base"
}, null, -1);

const _hoisted_14 = {
  class: "space-y-20 py-10 lg:px-12 xl:px-24"
};
const _hoisted_15 = {
  class: "mx-auto flex flex-col space-x-0 space-y-20 px-7 lg:flex-row lg:space-x-6 lg:space-y-0",
  style: {
    "max-width": "1280px"
  }
};
const _hoisted_16 = {
  class: "flex flex-1 flex-col space-y-20"
};
const _hoisted_17 = {
  class: "flex flex-1 flex-col space-y-4"
};
const _hoisted_18 = {
  class: "whitespace-pre-wrap text-left text-lg leading-relaxed tracking-wider"
};
const _hoisted_19 = {
  class: "text-6xl font-bold"
};

const _hoisted_20 = /*#__PURE__*/(0,runtime_core_esm_bundler/* createTextVNode */.Uk)("活動寫真");

const _hoisted_21 = {
  class: "flex w-full flex-col space-y-2 text-left lg:w-72"
};

const _hoisted_22 = /*#__PURE__*/(0,runtime_core_esm_bundler/* createElementVNode */._)("div", {
  class: "mb-5 text-left text-xl after:block after:w-16 after:border-b after:border-red-600 after:pt-1"
}, "其他消息", -1);

const _hoisted_23 = ["onClick"];
const _hoisted_24 = {
  class: "flex items-center text-sm text-gray-400"
};

const _hoisted_25 = /*#__PURE__*/(0,runtime_core_esm_bundler/* createElementVNode */._)("i", {
  class: "ri-time-fill mr-1"
}, null, -1);

function render(_ctx, _cache, $props, $setup, $data, $options) {
  const _component_router_link = (0,runtime_core_esm_bundler/* resolveComponent */.up)("router-link");

  const _component_a_breadcrumb_item = (0,runtime_core_esm_bundler/* resolveComponent */.up)("a-breadcrumb-item");

  const _component_a_breadcrumb = (0,runtime_core_esm_bundler/* resolveComponent */.up)("a-breadcrumb");

  const _component_a_image = (0,runtime_core_esm_bundler/* resolveComponent */.up)("a-image");

  const _component_a_divider = (0,runtime_core_esm_bundler/* resolveComponent */.up)("a-divider");

  const _component_Slide = (0,runtime_core_esm_bundler/* resolveComponent */.up)("Slide");

  const _component_Navigation = (0,runtime_core_esm_bundler/* resolveComponent */.up)("Navigation");

  const _component_Carousel = (0,runtime_core_esm_bundler/* resolveComponent */.up)("Carousel");

  return (0,runtime_core_esm_bundler/* openBlock */.wg)(), (0,runtime_core_esm_bundler/* createElementBlock */.iD)("div", _hoisted_1, [(0,runtime_core_esm_bundler/* createElementVNode */._)("div", _hoisted_2, [((0,runtime_core_esm_bundler/* openBlock */.wg)(true), (0,runtime_core_esm_bundler/* createElementBlock */.iD)(runtime_core_esm_bundler/* Fragment */.HY, null, (0,runtime_core_esm_bundler/* renderList */.Ko)($data.userData, item => {
    return (0,runtime_core_esm_bundler/* openBlock */.wg)(), (0,runtime_core_esm_bundler/* createElementBlock */.iD)("div", _hoisted_3, [(0,runtime_core_esm_bundler/* createVNode */.Wm)(_component_a_breadcrumb, {
      class: "mb-10 w-full text-left"
    }, {
      default: (0,runtime_core_esm_bundler/* withCtx */.w5)(() => [(0,runtime_core_esm_bundler/* createVNode */.Wm)(_component_a_breadcrumb_item, null, {
        default: (0,runtime_core_esm_bundler/* withCtx */.w5)(() => [(0,runtime_core_esm_bundler/* createVNode */.Wm)(_component_router_link, {
          to: "/"
        }, {
          default: (0,runtime_core_esm_bundler/* withCtx */.w5)(() => [_hoisted_4]),
          _: 1
        })]),
        _: 1
      }), (0,runtime_core_esm_bundler/* createVNode */.Wm)(_component_a_breadcrumb_item, null, {
        default: (0,runtime_core_esm_bundler/* withCtx */.w5)(() => [(0,runtime_core_esm_bundler/* createVNode */.Wm)(_component_router_link, {
          to: "/news"
        }, {
          default: (0,runtime_core_esm_bundler/* withCtx */.w5)(() => [_hoisted_5]),
          _: 1
        })]),
        _: 1
      }), (0,runtime_core_esm_bundler/* createVNode */.Wm)(_component_a_breadcrumb_item, null, {
        default: (0,runtime_core_esm_bundler/* withCtx */.w5)(() => [(0,runtime_core_esm_bundler/* createTextVNode */.Uk)((0,shared_esm_bundler/* toDisplayString */.zw)(item.title), 1)]),
        _: 2
      }, 1024)]),
      _: 2
    }, 1024), (0,runtime_core_esm_bundler/* createElementVNode */._)("p", _hoisted_6, (0,shared_esm_bundler/* toDisplayString */.zw)(item.title), 1), (0,runtime_core_esm_bundler/* createElementVNode */._)("div", _hoisted_7, [(0,runtime_core_esm_bundler/* createElementVNode */._)("p", _hoisted_8, [_hoisted_9, (0,runtime_core_esm_bundler/* createTextVNode */.Uk)(" " + (0,shared_esm_bundler/* toDisplayString */.zw)(item.time), 1)]), (0,runtime_core_esm_bundler/* createElementVNode */._)("p", _hoisted_10, [_hoisted_11, (0,runtime_core_esm_bundler/* createTextVNode */.Uk)(" " + (0,shared_esm_bundler/* toDisplayString */.zw)(item.location), 1)]), (0,runtime_core_esm_bundler/* createElementVNode */._)("p", _hoisted_12, [_hoisted_13, (0,runtime_core_esm_bundler/* createTextVNode */.Uk)(" " + (0,shared_esm_bundler/* toDisplayString */.zw)(item.user), 1)])])]);
  }), 256)), ((0,runtime_core_esm_bundler/* openBlock */.wg)(true), (0,runtime_core_esm_bundler/* createElementBlock */.iD)(runtime_core_esm_bundler/* Fragment */.HY, null, (0,runtime_core_esm_bundler/* renderList */.Ko)($data.userData, item => {
    return (0,runtime_core_esm_bundler/* openBlock */.wg)(), (0,runtime_core_esm_bundler/* createElementBlock */.iD)("div", _hoisted_14, [(0,runtime_core_esm_bundler/* createElementVNode */._)("div", _hoisted_15, [(0,runtime_core_esm_bundler/* createElementVNode */._)("div", _hoisted_16, [(0,runtime_core_esm_bundler/* createElementVNode */._)("div", _hoisted_17, [(0,runtime_core_esm_bundler/* createVNode */.Wm)(_component_a_image, {
      src: item.photo,
      width: "100%",
      height: "100%",
      class: "w-full rounded-lg object-cover",
      alt: ""
    }, null, 8, ["src"]), (0,runtime_core_esm_bundler/* createElementVNode */._)("div", _hoisted_18, (0,shared_esm_bundler/* toDisplayString */.zw)(item.article), 1)]), (0,runtime_core_esm_bundler/* createElementVNode */._)("div", _hoisted_19, [(0,runtime_core_esm_bundler/* createVNode */.Wm)(_component_a_divider, {
      orientation: "center"
    }, {
      default: (0,runtime_core_esm_bundler/* withCtx */.w5)(() => [_hoisted_20]),
      _: 1
    }), (0,runtime_core_esm_bundler/* createVNode */.Wm)(_component_Carousel, {
      itemsToShow: 3,
      wrapAround: true
    }, {
      addons: (0,runtime_core_esm_bundler/* withCtx */.w5)(() => [(0,runtime_core_esm_bundler/* createVNode */.Wm)(_component_Navigation)]),
      default: (0,runtime_core_esm_bundler/* withCtx */.w5)(() => [((0,runtime_core_esm_bundler/* openBlock */.wg)(true), (0,runtime_core_esm_bundler/* createElementBlock */.iD)(runtime_core_esm_bundler/* Fragment */.HY, null, (0,runtime_core_esm_bundler/* renderList */.Ko)(item.children, item => {
        return (0,runtime_core_esm_bundler/* openBlock */.wg)(), (0,runtime_core_esm_bundler/* createBlock */.j4)(_component_Slide, {
          key: item
        }, {
          default: (0,runtime_core_esm_bundler/* withCtx */.w5)(() => [(0,runtime_core_esm_bundler/* createVNode */.Wm)(_component_a_image, {
            class: "carousel__item",
            src: item.photo
          }, null, 8, ["src"])]),
          _: 2
        }, 1024);
      }), 128))]),
      _: 2
    }, 1024)])]), (0,runtime_core_esm_bundler/* createElementVNode */._)("div", _hoisted_21, [_hoisted_22, ((0,runtime_core_esm_bundler/* openBlock */.wg)(true), (0,runtime_core_esm_bundler/* createElementBlock */.iD)(runtime_core_esm_bundler/* Fragment */.HY, null, (0,runtime_core_esm_bundler/* renderList */.Ko)($data.newsList, item => {
      return (0,runtime_core_esm_bundler/* openBlock */.wg)(), (0,runtime_core_esm_bundler/* createElementBlock */.iD)("div", {
        class: "w-full cursor-pointer truncate rounded-md bg-white p-2 text-lg hover:bg-blue-100",
        onClick: (0,runtime_dom_esm_bundler/* withModifiers */.iM)($event => $options.passDataToB(item.id), ["prevent"])
      }, [(0,runtime_core_esm_bundler/* createTextVNode */.Uk)((0,shared_esm_bundler/* toDisplayString */.zw)(item.title) + " ", 1), (0,runtime_core_esm_bundler/* createElementVNode */._)("p", _hoisted_24, [_hoisted_25, (0,runtime_core_esm_bundler/* createTextVNode */.Uk)(" " + (0,shared_esm_bundler/* toDisplayString */.zw)(item.time), 1)])], 8, _hoisted_23);
    }), 256))])])]);
  }), 256))])]);
}
;// CONCATENATED MODULE: ./src/components/NewsContent2.vue?vue&type=template&id=62ffce64

// EXTERNAL MODULE: ./node_modules/core-js/modules/es.array.includes.js
var es_array_includes = __webpack_require__(6699);
// EXTERNAL MODULE: ./node_modules/@vue/reactivity/dist/reactivity.esm-bundler.js
var reactivity_esm_bundler = __webpack_require__(521);
;// CONCATENATED MODULE: ./node_modules/vue3-carousel/dist/carousel.es.js


/**
 * Vue 3 Carousel 0.1.40
 * (c) 2022
 * @license MIT
 */

const defaultConfigs = {
  itemsToShow: 1,
  itemsToScroll: 1,
  modelValue: 0,
  transition: 300,
  autoplay: 0,
  snapAlign: 'center',
  wrapAround: false,
  pauseAutoplayOnHover: false,
  mouseDrag: true,
  touchDrag: true,
  dir: 'ltr',
  breakpoints: undefined
};
/**
 * return a debounced version of the function
 * @param fn
 * @param delay
 */
// eslint-disable-next-line no-unused-vars

function debounce(fn, delay) {
  let timerId;
  return function (...args) {
    if (timerId) {
      clearTimeout(timerId);
    }

    timerId = setTimeout(() => {
      fn(...args);
      timerId = null;
    }, delay);
  };
}
/**
 * return a throttle version of the function
 * Throttling
 *
 */
// eslint-disable-next-line no-unused-vars


function throttle(fn, limit) {
  let inThrottle;
  return function (...args) {
    const self = this;

    if (!inThrottle) {
      fn.apply(self, args);
      inThrottle = true;
      setTimeout(() => inThrottle = false, limit);
    }
  };
}

function getSlidesVNodes(vNode) {
  var _a, _b, _c; // Return empty array if there's any node


  if (!vNode) return []; // Check if the Slides components are added directly without v-for (#72)

  if (((_b = (_a = vNode[0]) === null || _a === void 0 ? void 0 : _a.type) === null || _b === void 0 ? void 0 : _b.name) === 'CarouselSlide') return vNode;
  return ((_c = vNode[0]) === null || _c === void 0 ? void 0 : _c.children) || [];
}

function getMaxSlideIndex(config, slidesCount) {
  if (config.wrapAround) {
    return slidesCount - 1;
  }

  switch (config.snapAlign) {
    case 'start':
      return slidesCount - config.itemsToShow;

    case 'end':
      return slidesCount - 1;

    case 'center':
    case 'center-odd':
      return slidesCount - Math.ceil(config.itemsToShow / 2);

    case 'center-even':
      return slidesCount - Math.ceil(config.itemsToShow / 2);

    default:
      return 0;
  }
}

function getMinSlideIndex(config) {
  if (config.wrapAround) {
    return 0;
  }

  switch (config.snapAlign) {
    case 'start':
      return 0;

    case 'end':
      return config.itemsToShow - 1;

    case 'center':
    case 'center-odd':
      return Math.floor((config.itemsToShow - 1) / 2);

    case 'center-even':
      return Math.floor((config.itemsToShow - 2) / 2);

    default:
      return 0;
  }
}

function getCurrentSlideIndex(config, val, max, min) {
  if (config.wrapAround) {
    return val;
  }

  return Math.min(Math.max(val, min), max);
}

function getSlidesToScroll({
  slidesBuffer,
  currentSlide,
  snapAlign,
  itemsToShow,
  wrapAround,
  slidesCount
}) {
  let output = slidesBuffer.indexOf(currentSlide);

  if (output === -1) {
    output = slidesBuffer.indexOf(Math.ceil(currentSlide));
  }

  if (snapAlign === 'center' || snapAlign === 'center-odd') {
    output -= (itemsToShow - 1) / 2;
  } else if (snapAlign === 'center-even') {
    output -= (itemsToShow - 2) / 2;
  } else if (snapAlign === 'end') {
    output -= itemsToShow - 1;
  }

  if (!wrapAround) {
    const max = slidesCount - itemsToShow;
    const min = 0;
    output = Math.max(Math.min(output, max), min);
  }

  return output;
}

var Carousel = (0,runtime_core_esm_bundler/* defineComponent */.aZ)({
  name: 'Carousel',
  props: {
    // count of items to showed per view
    itemsToShow: {
      default: defaultConfigs.itemsToShow,
      type: Number
    },
    // count of items to be scrolled
    itemsToScroll: {
      default: defaultConfigs.itemsToScroll,
      type: Number
    },
    // control infinite scrolling mode
    wrapAround: {
      default: defaultConfigs.wrapAround,
      type: Boolean
    },
    // control snap position alignment
    snapAlign: {
      default: defaultConfigs.snapAlign,

      validator(value) {
        // The value must match one of these strings
        return ['start', 'end', 'center', 'center-even', 'center-odd'].includes(value);
      }

    },
    // sliding transition time in ms
    transition: {
      default: defaultConfigs.transition,
      type: Number
    },
    // an object to store breakpoints
    breakpoints: {
      default: defaultConfigs.breakpoints,
      type: Object
    },
    // time to auto advance slides in ms
    autoplay: {
      default: defaultConfigs.autoplay,
      type: Number
    },
    // pause autoplay when mouse hover over the carousel
    pauseAutoplayOnHover: {
      default: defaultConfigs.pauseAutoplayOnHover,
      type: Boolean
    },
    // slide number number of initial slide
    modelValue: {
      default: undefined,
      type: Number
    },
    // toggle mouse dragging.
    mouseDrag: {
      default: defaultConfigs.mouseDrag,
      type: Boolean
    },
    // toggle mouse dragging.
    touchDrag: {
      default: defaultConfigs.touchDrag,
      type: Boolean
    },
    // control snap position alignment
    dir: {
      default: defaultConfigs.dir,

      validator(value) {
        // The value must match one of these strings
        return ['rtl', 'ltr'].includes(value);
      }

    },
    // an object to pass all settings
    settings: {
      default() {
        return {};
      },

      type: Object
    }
  },

  setup(props, {
    slots,
    emit,
    expose
  }) {
    var _a;

    const root = (0,reactivity_esm_bundler/* ref */.iH)(null);
    const slides = (0,reactivity_esm_bundler/* ref */.iH)([]);
    const slidesBuffer = (0,reactivity_esm_bundler/* ref */.iH)([]);
    const slideWidth = (0,reactivity_esm_bundler/* ref */.iH)(0);
    const slidesCount = (0,reactivity_esm_bundler/* ref */.iH)(1);
    const autoplayTimer = (0,reactivity_esm_bundler/* ref */.iH)(null);
    const transitionTimer = (0,reactivity_esm_bundler/* ref */.iH)(null);
    let breakpoints = (0,reactivity_esm_bundler/* ref */.iH)({}); // generate carousel configs

    let __defaultConfig = Object.assign({}, defaultConfigs); // current config


    const config = (0,reactivity_esm_bundler/* reactive */.qj)(Object.assign({}, __defaultConfig)); // slides

    const currentSlideIndex = (0,reactivity_esm_bundler/* ref */.iH)((_a = config.modelValue) !== null && _a !== void 0 ? _a : 0);
    const prevSlideIndex = (0,reactivity_esm_bundler/* ref */.iH)(0);
    const middleSlideIndex = (0,reactivity_esm_bundler/* ref */.iH)(0);
    const maxSlideIndex = (0,reactivity_esm_bundler/* ref */.iH)(0);
    const minSlideIndex = (0,reactivity_esm_bundler/* ref */.iH)(0);
    (0,runtime_core_esm_bundler/* provide */.JJ)('config', config);
    (0,runtime_core_esm_bundler/* provide */.JJ)('slidesBuffer', slidesBuffer);
    (0,runtime_core_esm_bundler/* provide */.JJ)('slidesCount', slidesCount);
    (0,runtime_core_esm_bundler/* provide */.JJ)('currentSlide', currentSlideIndex);
    (0,runtime_core_esm_bundler/* provide */.JJ)('maxSlide', maxSlideIndex);
    (0,runtime_core_esm_bundler/* provide */.JJ)('minSlide', minSlideIndex);
    /**
     * Configs
     */

    function initDefaultConfigs() {
      // generate carousel configs
      const mergedConfigs = Object.assign(Object.assign({}, props), props.settings); // Set breakpoints

      breakpoints = (0,reactivity_esm_bundler/* ref */.iH)(Object.assign({}, mergedConfigs.breakpoints)); // remove extra values

      __defaultConfig = Object.assign(Object.assign({}, mergedConfigs), {
        settings: undefined,
        breakpoints: undefined
      });
      bindConfigs(__defaultConfig);
    }

    function updateBreakpointsConfigs() {
      const breakpointsArray = Object.keys(breakpoints.value).map(key => Number(key)).sort((a, b) => +b - +a);
      let newConfig = Object.assign({}, __defaultConfig);
      breakpointsArray.some(breakpoint => {
        const isMatched = window.matchMedia(`(min-width: ${breakpoint}px)`).matches;

        if (isMatched) {
          newConfig = Object.assign(Object.assign({}, newConfig), breakpoints.value[breakpoint]);
          return true;
        }

        return false;
      });
      bindConfigs(newConfig);
    }

    function bindConfigs(newConfig) {
      for (let key in newConfig) {
        // @ts-ignore
        config[key] = newConfig[key];
      }
    }

    const handleWindowResize = debounce(() => {
      if (breakpoints.value) {
        updateBreakpointsConfigs();
        updateSlidesData();
      }

      updateSlideWidth();
    }, 16);
    /**
     * Setup functions
     */

    function updateSlideWidth() {
      if (!root.value) return;
      const rect = root.value.getBoundingClientRect();
      slideWidth.value = rect.width / config.itemsToShow;
    }

    function updateSlidesData() {
      slidesCount.value = Math.max(slides.value.length, 1);
      if (slidesCount.value <= 0) return;
      middleSlideIndex.value = Math.ceil((slidesCount.value - 1) / 2);
      maxSlideIndex.value = getMaxSlideIndex(config, slidesCount.value);
      minSlideIndex.value = getMinSlideIndex(config);
      currentSlideIndex.value = getCurrentSlideIndex(config, currentSlideIndex.value, maxSlideIndex.value, minSlideIndex.value);
    }

    function updateSlidesBuffer() {
      const slidesArray = [...Array(slidesCount.value).keys()];
      const shouldShiftSlides = config.wrapAround && config.itemsToShow + 1 <= slidesCount.value;

      if (shouldShiftSlides) {
        const buffer = config.itemsToShow !== 1 ? Math.round((slidesCount.value - config.itemsToShow) / 2) : 0;
        let shifts = buffer - currentSlideIndex.value;

        if (config.snapAlign === 'end') {
          shifts += Math.floor(config.itemsToShow - 1);
        } else if (config.snapAlign === 'center' || config.snapAlign === 'center-odd') {
          shifts++;
        } // Check shifting directions


        if (shifts < 0) {
          for (let i = shifts; i < 0; i++) {
            slidesArray.push(Number(slidesArray.shift()));
          }
        } else {
          for (let i = 0; i < shifts; i++) {
            slidesArray.unshift(Number(slidesArray.pop()));
          }
        }
      }

      slidesBuffer.value = slidesArray;
    }

    (0,runtime_core_esm_bundler/* onMounted */.bv)(() => {
      if (breakpoints.value) {
        updateBreakpointsConfigs();
        updateSlidesData();
      }

      (0,runtime_core_esm_bundler/* nextTick */.Y3)(() => setTimeout(updateSlideWidth, 16));

      if (config.autoplay && config.autoplay > 0) {
        initializeAutoplay();
      }

      window.addEventListener('resize', handleWindowResize, {
        passive: true
      });
    });
    (0,runtime_core_esm_bundler/* onUnmounted */.Ah)(() => {
      if (transitionTimer.value) {
        clearTimeout(transitionTimer.value);
      }

      resetAutoplayTimer(false);
    });
    /**
     * Carousel Event listeners
     */

    let isTouch = false;
    const startPosition = {
      x: 0,
      y: 0
    };
    const endPosition = {
      x: 0,
      y: 0
    };
    const dragged = (0,reactivity_esm_bundler/* reactive */.qj)({
      x: 0,
      y: 0
    });
    const isDragging = (0,reactivity_esm_bundler/* ref */.iH)(false);
    const isHover = (0,reactivity_esm_bundler/* ref */.iH)(false);

    const handleMouseEnter = () => {
      isHover.value = true;
    };

    const handleMouseLeave = () => {
      isHover.value = false;
    };

    function handleDragStart(event) {
      isTouch = event.type === 'touchstart';

      if (!isTouch && event.button !== 0 || isSliding.value) {
        return;
      }

      isDragging.value = true;
      startPosition.x = isTouch ? event.touches[0].clientX : event.clientX;
      startPosition.y = isTouch ? event.touches[0].clientY : event.clientY;
      document.addEventListener(isTouch ? 'touchmove' : 'mousemove', handleDragging, true);
      document.addEventListener(isTouch ? 'touchend' : 'mouseup', handleDragEnd, true);
    }

    const handleDragging = throttle(event => {
      endPosition.x = isTouch ? event.touches[0].clientX : event.clientX;
      endPosition.y = isTouch ? event.touches[0].clientY : event.clientY;
      const deltaX = endPosition.x - startPosition.x;
      const deltaY = endPosition.y - startPosition.y;
      dragged.y = deltaY;
      dragged.x = deltaX;
    }, 16);

    function handleDragEnd() {
      isDragging.value = false;
      const direction = config.dir === 'rtl' ? -1 : 1;
      const tolerance = Math.sign(dragged.x) * 0.4;
      const draggedSlides = Math.round(dragged.x / slideWidth.value + tolerance) * direction;
      let newSlide = getCurrentSlideIndex(config, currentSlideIndex.value - draggedSlides, maxSlideIndex.value, minSlideIndex.value); // Prevent clicking if there is clicked slides

      if (draggedSlides) {
        const captureClick = e => {
          e.stopPropagation();
          window.removeEventListener('click', captureClick, true);
        };

        window.addEventListener('click', captureClick, true);
      }

      slideTo(newSlide);
      dragged.x = 0;
      dragged.y = 0;
      document.removeEventListener(isTouch ? 'touchmove' : 'mousemove', handleDragging, true);
      document.removeEventListener(isTouch ? 'touchend' : 'mouseup', handleDragEnd, true);
    }
    /**
     * Autoplay
     */


    function initializeAutoplay() {
      autoplayTimer.value = setInterval(() => {
        if (config.pauseAutoplayOnHover && isHover.value) {
          return;
        }

        next();
      }, config.autoplay);
    }

    function resetAutoplayTimer(restart = true) {
      if (!autoplayTimer.value) {
        return;
      }

      clearInterval(autoplayTimer.value);

      if (restart) {
        initializeAutoplay();
      }
    }
    /**
     * Navigation function
     */


    const isSliding = (0,reactivity_esm_bundler/* ref */.iH)(false);

    function slideTo(slideIndex, mute = false) {
      resetAutoplayTimer();

      if (currentSlideIndex.value === slideIndex || isSliding.value) {
        return;
      } // Wrap slide index


      const lastSlideIndex = slidesCount.value - 1;

      if (slideIndex > lastSlideIndex) {
        return slideTo(slideIndex - slidesCount.value);
      }

      if (slideIndex < 0) {
        return slideTo(slideIndex + slidesCount.value);
      }

      isSliding.value = true;
      prevSlideIndex.value = currentSlideIndex.value;
      currentSlideIndex.value = slideIndex;

      if (!mute) {
        emit('update:modelValue', currentSlideIndex.value);
      }

      transitionTimer.value = setTimeout(() => {
        if (config.wrapAround) updateSlidesBuffer();
        isSliding.value = false;
      }, config.transition);
    }

    function next() {
      let nextSlide = currentSlideIndex.value + config.itemsToScroll;

      if (!config.wrapAround) {
        nextSlide = Math.min(nextSlide, maxSlideIndex.value);
      }

      slideTo(nextSlide);
    }

    function prev() {
      let prevSlide = currentSlideIndex.value - config.itemsToScroll;

      if (!config.wrapAround) {
        prevSlide = Math.max(prevSlide, minSlideIndex.value);
      }

      slideTo(prevSlide);
    }

    const nav = {
      slideTo,
      next,
      prev
    };
    (0,runtime_core_esm_bundler/* provide */.JJ)('nav', nav);
    /**
     * Track style
     */

    const slidesToScroll = (0,runtime_core_esm_bundler/* computed */.Fl)(() => getSlidesToScroll({
      slidesBuffer: slidesBuffer.value,
      itemsToShow: config.itemsToShow,
      snapAlign: config.snapAlign,
      wrapAround: Boolean(config.wrapAround),
      currentSlide: currentSlideIndex.value,
      slidesCount: slidesCount.value
    }));
    (0,runtime_core_esm_bundler/* provide */.JJ)('slidesToScroll', slidesToScroll);
    const trackStyle = (0,runtime_core_esm_bundler/* computed */.Fl)(() => {
      const direction = config.dir === 'rtl' ? -1 : 1;
      const xScroll = slidesToScroll.value * slideWidth.value * direction;
      return {
        transform: `translateX(${dragged.x - xScroll}px)`,
        transition: `${isSliding.value ? config.transition : 0}ms`
      };
    });

    function initCarousel() {
      initDefaultConfigs();
    }

    function restartCarousel() {
      initDefaultConfigs();
      updateBreakpointsConfigs();
      updateSlidesData();
      updateSlidesBuffer();
      updateSlideWidth();
    }

    function updateCarousel() {
      updateSlidesData();
      updateSlidesBuffer();
    } // Update the carousel on props change


    (0,runtime_core_esm_bundler/* watch */.YP)(() => Object.values(props), restartCarousel); // Init carousel

    initCarousel();
    (0,runtime_core_esm_bundler/* watchEffect */.m0)(() => {
      // Handel when slides added/removed
      const needToUpdate = slidesCount.value !== slides.value.length;
      const currentSlideUpdated = props.modelValue !== undefined && currentSlideIndex.value !== props.modelValue;

      if (currentSlideUpdated) {
        slideTo(Number(props.modelValue), true);
      }

      if (needToUpdate) {
        updateCarousel();
      }
    });
    const data = {
      config,
      slidesBuffer,
      slidesCount,
      slideWidth,
      currentSlide: currentSlideIndex,
      maxSlide: maxSlideIndex,
      minSlide: minSlideIndex,
      middleSlide: middleSlideIndex
    };
    expose({
      updateBreakpointsConfigs,
      updateSlidesData,
      updateSlideWidth,
      updateSlidesBuffer,
      initCarousel,
      restartCarousel,
      updateCarousel,
      slideTo,
      next,
      prev,
      nav,
      data
    });
    const slotSlides = slots.default || slots.slides;
    const slotAddons = slots.addons;
    const slotsProps = (0,reactivity_esm_bundler/* reactive */.qj)(data);
    return () => {
      const slidesElements = getSlidesVNodes(slotSlides === null || slotSlides === void 0 ? void 0 : slotSlides(slotsProps));
      const addonsElements = (slotAddons === null || slotAddons === void 0 ? void 0 : slotAddons(slotsProps)) || [];
      slides.value = slidesElements; // Bind slide order

      slidesElements.forEach((el, index) => el.props.index = index);
      const trackEl = (0,runtime_core_esm_bundler.h)('ol', {
        class: 'carousel__track',
        style: trackStyle.value,
        onMousedown: config.mouseDrag ? handleDragStart : null,
        onTouchstart: config.touchDrag ? handleDragStart : null
      }, slidesElements);
      const viewPortEl = (0,runtime_core_esm_bundler.h)('div', {
        class: 'carousel__viewport'
      }, trackEl);
      return (0,runtime_core_esm_bundler.h)('section', {
        ref: root,
        class: {
          carousel: true,
          'carousel--rtl': config.dir === 'rtl'
        },
        dir: config.dir,
        'aria-label': 'Gallery',
        onMouseenter: handleMouseEnter,
        onMouseleave: handleMouseLeave
      }, [viewPortEl, addonsElements]);
    };
  }

});
const icons = {
  arrowUp: 'M7.41 15.41L12 10.83l4.59 4.58L18 14l-6-6-6 6z',
  arrowDown: 'M7.41 8.59L12 13.17l4.59-4.58L18 10l-6 6-6-6 1.41-1.41z',
  arrowRight: 'M8.59 16.59L13.17 12 8.59 7.41 10 6l6 6-6 6-1.41-1.41z',
  arrowLeft: 'M15.41 16.59L10.83 12l4.58-4.59L14 6l-6 6 6 6 1.41-1.41z'
};

const Icon = props => {
  const iconName = props.name;

  if (!iconName || typeof iconName !== 'string') {
    return;
  }

  const path = icons[iconName];
  const pathEl = (0,runtime_core_esm_bundler.h)('path', {
    d: path
  });
  const iconTitle = props.title || iconName;
  const titleEl = (0,runtime_core_esm_bundler.h)('title', null, iconName);
  return (0,runtime_core_esm_bundler.h)('svg', {
    class: 'carousel__icon',
    viewBox: '0 0 24 24',
    role: 'img',
    ariaLabel: iconTitle
  }, [titleEl, pathEl]);
};

Icon.props = {
  name: String,
  title: String
};

const Navigation = (props, {
  slots,
  attrs
}) => {
  const {
    next: slotNext,
    prev: slotPrev
  } = slots;
  const config = (0,runtime_core_esm_bundler/* inject */.f3)('config', (0,reactivity_esm_bundler/* reactive */.qj)(Object.assign({}, defaultConfigs)));
  const maxSlide = (0,runtime_core_esm_bundler/* inject */.f3)('maxSlide', (0,reactivity_esm_bundler/* ref */.iH)(1));
  const minSlide = (0,runtime_core_esm_bundler/* inject */.f3)('minSlide', (0,reactivity_esm_bundler/* ref */.iH)(1));
  const currentSlide = (0,runtime_core_esm_bundler/* inject */.f3)('currentSlide', (0,reactivity_esm_bundler/* ref */.iH)(1));
  const nav = (0,runtime_core_esm_bundler/* inject */.f3)('nav', {});
  const isRTL = config.dir === 'rtl';
  const prevButton = (0,runtime_core_esm_bundler.h)('button', {
    type: 'button',
    class: ['carousel__prev', !config.wrapAround && currentSlide.value <= minSlide.value && 'carousel__prev--in-active', attrs === null || attrs === void 0 ? void 0 : attrs.class],
    'aria-label': `Navigate to previous slide`,
    onClick: nav.prev
  }, (slotPrev === null || slotPrev === void 0 ? void 0 : slotPrev()) || (0,runtime_core_esm_bundler.h)(Icon, {
    name: isRTL ? 'arrowRight' : 'arrowLeft'
  }));
  const nextButton = (0,runtime_core_esm_bundler.h)('button', {
    type: 'button',
    class: ['carousel__next', !config.wrapAround && currentSlide.value >= maxSlide.value && 'carousel__next--in-active', attrs === null || attrs === void 0 ? void 0 : attrs.class],
    'aria-label': `Navigate to next slide`,
    onClick: nav.next
  }, (slotNext === null || slotNext === void 0 ? void 0 : slotNext()) || (0,runtime_core_esm_bundler.h)(Icon, {
    name: isRTL ? 'arrowLeft' : 'arrowRight'
  }));
  return [prevButton, nextButton];
};

var Slide = (0,runtime_core_esm_bundler/* defineComponent */.aZ)({
  name: 'CarouselSlide',
  props: {
    index: {
      type: Number,
      default: 1
    }
  },

  setup(props, {
    slots
  }) {
    const config = (0,runtime_core_esm_bundler/* inject */.f3)('config', (0,reactivity_esm_bundler/* reactive */.qj)(Object.assign({}, defaultConfigs)));
    const slidesBuffer = (0,runtime_core_esm_bundler/* inject */.f3)('slidesBuffer', (0,reactivity_esm_bundler/* ref */.iH)([]));
    const currentSlide = (0,runtime_core_esm_bundler/* inject */.f3)('currentSlide', (0,reactivity_esm_bundler/* ref */.iH)(0));
    const slidesToScroll = (0,runtime_core_esm_bundler/* inject */.f3)('slidesToScroll', (0,reactivity_esm_bundler/* ref */.iH)(0));
    const wrapOrder = (0,reactivity_esm_bundler/* ref */.iH)(props.index);

    if (config.wrapAround) {
      updateOrder();
      (0,runtime_core_esm_bundler/* watch */.YP)(slidesBuffer, updateOrder);
    }

    function updateOrder() {
      wrapOrder.value = slidesBuffer.value.indexOf(props.index);
    }

    const slideStyle = (0,runtime_core_esm_bundler/* computed */.Fl)(() => {
      const items = config.itemsToShow;
      const width = `${1 / items * 100}%`;
      return {
        width,
        order: wrapOrder.value.toString()
      };
    });

    const isActive = () => props.index === currentSlide.value;

    const isVisible = () => {
      const min = Math.ceil(slidesToScroll.value);
      const max = Math.floor(slidesToScroll.value + config.itemsToShow);
      const current = slidesBuffer.value.slice(min, max);
      return current.includes(props.index);
    };

    const isPrev = () => props.index === slidesBuffer.value[Math.ceil(slidesToScroll.value) - 1];

    const isNext = () => props.index === slidesBuffer.value[Math.floor(slidesToScroll.value + config.itemsToShow)];

    return () => {
      var _a;

      return (0,runtime_core_esm_bundler.h)('li', {
        style: slideStyle.value,
        class: {
          carousel__slide: true,
          'carousel__slide--active': isActive(),
          'carousel__slide--visible': isVisible(),
          'carousel__slide--prev': isPrev(),
          'carousel__slide--next': isNext()
        }
      }, (_a = slots.default) === null || _a === void 0 ? void 0 : _a.call(slots));
    };
  }

});

const Pagination = () => {
  const maxSlide = (0,runtime_core_esm_bundler/* inject */.f3)('maxSlide', (0,reactivity_esm_bundler/* ref */.iH)(1));
  const minSlide = (0,runtime_core_esm_bundler/* inject */.f3)('minSlide', (0,reactivity_esm_bundler/* ref */.iH)(1));
  const currentSlide = (0,runtime_core_esm_bundler/* inject */.f3)('currentSlide', (0,reactivity_esm_bundler/* ref */.iH)(1));
  const nav = (0,runtime_core_esm_bundler/* inject */.f3)('nav', {});

  function handleButtonClick(slideNumber) {
    nav.slideTo(slideNumber);
  }

  const isActive = slide => {
    const val = currentSlide.value;
    return val === slide || val > maxSlide.value && slide >= maxSlide.value || val < minSlide.value && slide <= minSlide.value;
  };

  const children = [];

  for (let slide = minSlide.value; slide < maxSlide.value + 1; slide++) {
    const button = (0,runtime_core_esm_bundler.h)('button', {
      type: 'button',
      class: {
        'carousel__pagination-button': true,
        'carousel__pagination-button--active': isActive(slide)
      },
      'aria-label': `Navigate to slide ${slide + 1}`,
      onClick: () => handleButtonClick(slide)
    });
    const item = (0,runtime_core_esm_bundler.h)('li', {
      class: 'carousel__pagination-item',
      key: slide
    }, button);
    children.push(item);
  }

  return (0,runtime_core_esm_bundler.h)('ol', {
    class: 'carousel__pagination'
  }, children);
};


// EXTERNAL MODULE: ./static/news.json
var news = __webpack_require__(8579);
;// CONCATENATED MODULE: ./node_modules/babel-loader/lib/index.js??clonedRuleSet-40.use[0]!./node_modules/vue-loader/dist/index.js??ruleSet[0].use[0]!./src/components/NewsContent2.vue?vue&type=script&lang=js



/* harmony default export */ var NewsContent2vue_type_script_lang_js = ({
  name: "NewsContent",
  components: {
    Carousel: Carousel,
    Slide: Slide,
    Pagination: Pagination,
    Navigation: Navigation
  },

  data() {
    return {
      id: this.$route.query.id,
      userData: [],
      newsList: news
    };
  },

  computed: {},

  created() {
    this.getInfo();
  },

  mounted() {},

  methods: {
    getInfo() {
      this.$http.get("assets/api/news/" + this.id + ".json").then(res => {
        this.userData = res.data;
      });
    },

    passDataToB(id) {
      this.$router.push({
        name: "news_details",
        query: {
          id
        }
      });
    }

  }
});
;// CONCATENATED MODULE: ./src/components/NewsContent2.vue?vue&type=script&lang=js
 
;// CONCATENATED MODULE: ./node_modules/mini-css-extract-plugin/dist/loader.js??clonedRuleSet-22.use[0]!./node_modules/css-loader/dist/cjs.js??clonedRuleSet-22.use[1]!./node_modules/vue-loader/dist/stylePostLoader.js!./node_modules/postcss-loader/dist/cjs.js??clonedRuleSet-22.use[2]!./node_modules/sass-loader/dist/cjs.js??clonedRuleSet-22.use[3]!./node_modules/vue-loader/dist/index.js??ruleSet[0].use[0]!./src/components/NewsContent2.vue?vue&type=style&index=0&id=62ffce64&lang=scss
// extracted by mini-css-extract-plugin

;// CONCATENATED MODULE: ./src/components/NewsContent2.vue?vue&type=style&index=0&id=62ffce64&lang=scss

// EXTERNAL MODULE: ./node_modules/vue-loader/dist/exportHelper.js
var exportHelper = __webpack_require__(8998);
;// CONCATENATED MODULE: ./src/components/NewsContent2.vue




;


const __exports__ = /*#__PURE__*/(0,exportHelper/* default */.Z)(NewsContent2vue_type_script_lang_js, [['render',render]])

/* harmony default export */ var NewsContent2 = (__exports__);

/***/ }),

/***/ 8579:
/***/ (function(module) {

module.exports = JSON.parse('[{"id":0,"title":"數位建築學院BIM+ Studio啟用暨MOU簽約典禮","photo":"./assets/news/news1/news-p0.jpeg","time":"2019/11/25","user":"財團法人台灣建築中心","location":"新北市新店區","article":"財團法人台灣建築中心(TABC)為落實BIM產業推動與發展，於11/25 正式啟用BIM+ Studio。BIM+ Studio 除了提供良好的場域可進行work shop與教育訓練之外，未來將協助推動BIM產業、物聯網、數位雙生(Digital Twin)、與智慧城市之執行，扮演AEC產業輔導之角色。 透過本次活動，建築中心也分別與「互動國際數位股份有限公司」和「億集創見應用科技股份有限公司」簽署合作備忘錄(MOU)，以建立BIM未來於智慧設施營運管理和地理資訊(GIS)之整合應用趨勢。","children":[{"id":1,"activity_photo":"./assets/news/news1/news-p1.jpeg"},{"id":2,"activity_photo":"./assets/news/news1/news-p2.jpeg"},{"id":3,"activity_photo":"./assets/news/news1/news-p3.jpeg"},{"id":4,"activity_photo":"./assets/news/news1/news-p4.jpeg"},{"id":5,"activity_photo":"./assets/news/news1/news-p5.jpeg"},{"id":6,"activity_photo":"./assets/news/news1/news-p6.jpeg"}]},{"id":1,"title":"2019台灣國際人工智慧暨物聯網展","photo":"./assets/news/news2/news-p0.jpeg","time":"2019.10.16~2019.10.18","user":"中華民國對外貿易發展協會","location":"台北南港展覽館1館","article":"「智慧城鄉」乃運用各式智慧科技(如物聯網、大數據、人工智慧)，活用新資源(Data)有效滿足融合來自市民、產業與地方到中央政府多元「高度差異化」需求，加速城市與鄉鎮間智慧服務的協作及融合，落實在地實踐與產業創新。 #AIoT應用館 希望透過體驗展示，將 #智慧城鄉計畫 優質案例向海內外參觀團體分享計畫執行成果，更希望藉由此次展示，促進應用方案輸出國際，創造資通訊產業商機。在AIoT 應用館，你可親自體驗與發掘更多用科技寫的日常生活故事。 10/16 在南港展覽館的台灣國際人工智慧暨物聯網展 (AIoT Taiwan)，邀請大家到工業局智慧城鄉AIoT 應用館(K607)，來體驗智慧服務，感受創新好生活。","children":[{"id":1,"activity_photo":"./assets/news/news2/news-p1.jpeg"},{"id":2,"activity_photo":"./assets/news/news2/news-p2.jpeg"},{"id":3,"activity_photo":"./assets/news/news2/news-p3.jpeg"},{"id":4,"activity_photo":"./assets/news/news2/news-p4.jpeg"},{"id":5,"activity_photo":"./assets/news/news2/news-p5.jpeg"},{"id":6,"activity_photo":"./assets/news/news2/news-p6.jpeg"}]},{"id":2,"title":"數位建築學院BIM+ Studio啟用暨MOU簽約典禮","photo":"./assets/news/news3/news-p0.jpeg","time":"2019.07.01~2019.08.29","user":"內政部建築研究所","location":"北中南","article":"一、 計畫緣起\\n 本次講習會之議程規劃以宣導近年 BIM 研究成果及應用情形為主軸，輔以國 內營建工程 BIM 本土化發展技術分享，安排國內應用 BIM 技術之專家學者進行 講習與經驗傳承。講習內容包含由建築研究所對台灣 BIM 的發展趨勢及未來展望， 以及其次由 BSI 英國標準協會所發布的國際標準 ISO19650 中文版，其次介紹業界 之建築工程導入 BIM 關鍵及如何提升作業效益及管理經驗分享，以及應用 BIM 方 式來輔助整合並且提高工作效率等，由專家學者分別從不同專業面向切入分享 BIM 本土化應用實務，期帶領國內公部門、營建業從專案規劃開始深入了解如何 將 BIM 導入專案中，如何運用 BIM 執行管理，以提升設計效率與施工品質，有效 利用 BIM 的優勢至整個建築專案生命週期。\\n 二、 預期效益(在地化、本土化應用)\\n 近年國內積極導入 BIM 技術，成果累積漸豐。臺灣業界應用層面已從早期的 設計施工，進步到現在營運資訊的管理，真正做到建築生命周期的全盤考量。盤點 目前臺灣 BIM 推廣上的困境，包括設計、施工至營運端對其所需 BIM 訊息的需求 不明，國內亦欠缺 BIM 規模化推進之本土化策略和未來發展之遠見，應用軟體間 檔案移轉受到限制，以及資料建置尚無統一標準格式，導致專案人員重複建置資料 的資源浪費，期盼產、官、學界能著眼於此加速改善。\\n 本次講習會擬透過國內業界累積至目前豐碩成果的展示及業界應用的經驗分 享，除讓大眾了解目前政府單位推動 BIM 的趨勢方向與決心，更希冀透過建築產 業中不同角色不同出發點的應用經驗分享，突破您對 BIM 的認識，激發國內 AEC 產業在 BIM 技術應用創造更多發展的可能性，藉此研討會促成產官學三方交流， 各界能持續深化己身 BIM 技術工具，並帶入全球新視野，齊肩併進協助臺灣營建 產業的發展。","children":[{"id":1,"activity_photo":"./assets/news/news3/news-p1.jpeg"},{"id":2,"activity_photo":"./assets/news/news3/news-p2.jpeg"},{"id":3,"activity_photo":"./assets/news/news3/news-p3.jpeg"},{"id":4,"activity_photo":"./assets/news/news3/news-p4.jpeg"},{"id":5,"activity_photo":"./assets/news/news3/news-p5.jpeg"},{"id":6,"activity_photo":"./assets/news/news3/news-p6.jpeg"}]},{"id":3,"title":"2018台北101智慧趨勢展","photo":"./assets/news/news4/news-p0.jpeg","time":"2018.10.16~2018.10.19","user":"台北101","location":"台北101","article":"台北101 作為讓世界看見台灣的門戶與企業公民典範，一直以來致力於綠色、環保、永續等議題以及社會參與，並以「全球超高綠建築典範」以及「最高公益平台」作為目標。而在巨量資料、雲端運算與物聯網等科技出現後， 台北 101 該做的不僅是引進科技來增進自身在永續議題與社會責任上的付出，更應透過自身角色與能量，驅動城市革新與產業發展。於是，在這樣的目標與願景中，「台北101智慧趨勢展」，應運而生。 台北101將於2018年10月16日舉辦 「台北101智慧趨勢展」 ，以全球超高綠建築的智慧升級為目標，擴展國際級綠建築與智慧建築平台之產官學網絡，為國內外科技創新夥伴提供展示舞台以及接軌國際的機會，展出內容更包含國內外的創新科技，精彩可期，敬邀您一同參與。","children":[{"id":1,"activity_photo":"./assets/news/news4/news-p1.jpeg"},{"id":2,"activity_photo":"./assets/news/news4/news-p2.jpeg"}]},{"id":4,"title":"世界華人不動產學會暨亞洲不動產學會2017聯合年會","photo":"./assets/news/news5/news-p0.jpeg","time":"2017.07.09~2017.07.12","user":"世界不動產、亞州不動產(學會)","location":"台中林酒店","article":"世界華人不動產學會與亞洲不動產學會共同舉辦的2017聯合年會，在台中林酒店進行了為期4天的論壇研討會，其中也邀請了諾貝爾經濟學獎2013年得主的Robert J. Shiller擔任主講嘉賓，分享房地產市場經濟發展與趨勢。億集創見科技受到主辦單位委託，負責開發此次大會活動的APP，希望讓參加活動的來賓能隨時透過行動APP掌握活動最新資訊和論壇消息，並透過室內導航的功能，協助來賓前往舉辦演講的空間。 億集創見科技成立於2008年，將BIM 3D模型結合混合實境(Mixed Reality, MR)和空間定位技術導入建築產業，提供前期規劃設計、中期營造施工、後期營運管理各階段作業需求的軟體服務方案。此外，包含台中七期豪宅的社區住戶服務APP、物聯網智慧建築和Beacon ERP雲端考勤也都是該公司軟體服務強項。","children":[{"id":1,"activity_photo":"./assets/news/news5/news-p1.jpeg"},{"id":2,"activity_photo":"./assets/news/news5/news-p2.jpeg"},{"id":3,"activity_photo":"./assets/news/news5/news-p3.jpeg"},{"id":4,"activity_photo":"./assets/news/news5/news-p4.jpeg"},{"id":5,"activity_photo":"./assets/news/news5/news-p5.jpeg"},{"id":6,"activity_photo":"./assets/news/news5/news-p6.jpeg"}]},{"id":5,"title":"2017數位建築BIM智慧城市展","photo":"./assets/news/news6/news-p0.jpeg","time":"2017.01.13","user":"億集創見應用科技","location":"逢甲大學","article":"當日特別到場嘉賓：林佳龍","children":[{"id":1,"activity_photo":"./assets/news/news6/news-p1.jpeg"},{"id":2,"activity_photo":"./assets/news/news6/news-p2.jpeg"},{"id":3,"activity_photo":"./assets/news/news6/news-p3.jpeg"},{"id":4,"activity_photo":"./assets/news/news6/news-p4.jpeg"},{"id":5,"activity_photo":"./assets/news/news6/news-p5.jpeg"},{"id":6,"activity_photo":"./assets/news/news6/news-p6.jpeg"}]}]');

/***/ })

}]);